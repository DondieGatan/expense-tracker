@model ExpenseTracker.Models.Expense
@{
    ViewData["Title"] = "Edit Expense";
    var categories = (string[])ViewBag.Categories;
    var paymentMethods = (string[])ViewBag.PaymentMethods;
}

<h1 class="page-title">Edit Expense</h1>

<form asp-action="Edit" method="post" class="form-panel">
    <input type="hidden" asp-for="Id">
    <div asp-validation-summary="ModelOnly" class="validation-summary-errors"></div>

    <label>Description
        <input asp-for="Description" required maxlength="150">
        <span asp-validation-for="Description" class="field-validation-error"></span>
    </label>

    <div class="field-row">
        <label>Amount
            <input asp-for="Amount" type="number" step="0.01" min="0.01" required>
            <span asp-validation-for="Amount" class="field-validation-error"></span>
        </label>
        <label>Date
            <input asp-for="Date" type="date" required>
        </label>
    </div>

    <div class="field-row">
        <label>Category
            <select asp-for="Category">
                @foreach (var c in categories)
                {
                    <option value="@c">@c</option>
                }
            </select>
        </label>
        <label>Payment Method
            <select asp-for="PaymentMethod">
                @foreach (var p in paymentMethods)
                {
                    <option value="@p">@p</option>
                }
            </select>
        </label>
    </div>

    <label>Notes
        <textarea asp-for="Notes" rows="3"></textarea>
    </label>

    <div class="form-actions">
        <button type="submit" class="btn btn-primary">Save Changes</button>
        <a asp-action="Index" class="btn btn-ghost">Cancel</a>
    </div>
</form>
